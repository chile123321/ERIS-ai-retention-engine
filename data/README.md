# Local data

Place authorized files in `raw/`, derived intermediate files in `interim/`, modeling-ready files in `processed/`, and the immutable supplied splits in `splits/`. Contents are Git-ignored. Never recreate or inspect the locked final test during development.
