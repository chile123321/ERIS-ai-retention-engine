# Data contract

The versioned machine-readable contract is `configs/data/data_contract_v1.yaml`. It will define column types, allowed values, nullability, ranges, target mapping, and logical constraints after the authoritative audit is reviewed. Identifiers must be validated where needed and excluded from model inputs.
